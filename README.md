# ownexecutor

make exec to 1 exec 
so your code of script
will be working on every other exec.

```lua
local ownexec = loadstring("https://raw.githubusercontent.com/igorgames-htm/ownexecutor/main/main.luau")()
```
